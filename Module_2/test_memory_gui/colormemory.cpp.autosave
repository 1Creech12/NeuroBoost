#include "colormemory.h"

ColorMemory::ColorMemory(QObject *parent)
    : QObject(parent)
    , currentIndex(0) {}


void ColorMemory::addColor() {
    QVector<QString> colors = {"red", "blue", "yellow", "green"};
    int index = rand() % 4;
    sequence.push_back(colors[index]);
}

void ColorMemory::checkColor(const QString& playerColor) {
    if (sequence.size() <= currentIndex) return;
    
    if (sequence[currentIndex] == playerColor) {
        у
    }
}

/*
void ColorMemory::game() {
    
    int record = 0;
    while (true) {
        this->addColor();
        
        ui->colorsText->setText(sequence.join(' '));
        
        int index = 0;
        bool roundFailed = false;
        
        while (index < sequence.size()) {  
            
            QString playerColor;
                      
            if (game.isCorrect(index, playerColor)) {
                cout << "Correct" << endl;
                index++;
                record++;
            } else {
                cout << "Uncorrect" << endl;
                
                cout << "Game over" << endl;
                cout << "Your record: " << record << endl;
                roundFailed = true;
                break;
            }
        }
        
        if (roundFailed) break;
        cout << "Level complete, please Enter, to go further...";
    }
}
*/
